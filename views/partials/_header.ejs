<header>
  <h2>The ugliest but coolest URL shortener <3 </h2>
    <% if(user){ %>
      <h3>Welcome <%= user.id %> <a href='/logout'>Logout</a></h3>
    <% } else { %> 
      <a href = '/login'>Login</a> 
      <a href = '/register'>Register</a>
    <% } %>
</header>