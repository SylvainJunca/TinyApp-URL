<!DOCTYPE html>
<html lang="en">
<head>
  <title>TinyApp</title>
</head>
<body>
    <header>
        <% include ./partials/_header %>
    </header>
    
    <ul>
      <% for (const each in urls){ %>
      <form method="POST" action='/urls/<%= each %>/delete'>
      <li>
        <a href = '/urls/<%= each %>'> <%= each %> </a> : <%= urls[each]['url'] %>
          <input type = "submit" value="Delete">
      </li>
    </form>
    <% }; %> 
  </ul>
  
  <a href='/urls/new'>Shorten a new URL</a>
</body>
</html>